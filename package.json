{
    "name": "my-project",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently -n \"client,server\" -c \"bgBlue,bgYellow\" \"npm start --prefix client\" \"npm start --prefix server\"",
        "client": "npm start --prefix client",
        "server": "npm start --prefix server"
    },
    "devDependencies": {
        "concurrently": "^7.6.0"
    }
}
